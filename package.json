{
  "name": "@gwiyeomgo/active-button-by-status",
  "version": "0.0.0",
  "module": "dist/ActiveButtonByStatus.js",
  "main": "dist/ActiveButtonByStatus.js",
  "description": "react button component that are enabled and disabled depending on their status",
  "keywords": [
    "react",
    "button",
    "status"
  ],
  "homepage": "https://github.com/gwiyeomgo/active-button-by-status",
  "bugs": {
    "url": "https://github.com/gwiyeomgo/active-button-by-status/issues",
    "email": "gwiyeomgo@gmail.com"
  },
  "author": {
    "email": "gwiyeomgo@gmail.com",
    "name": "gwiyeomgo"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "rimraf": "^3.0.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "publish:npm": "npx rimraf dist && mkdir dist && babel src/lib -d dist --copy-files"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "babel": {
    "presets": [
      "@babel/preset-react"
    ]
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "devDependencies": {
    "@babel/cli": "^7.18.10",
    "@babel/preset-react": "^7.18.6"
  }
}
